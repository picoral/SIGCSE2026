Evaluate the code below. Write down what the last line of code in each chunk prints. When the code throws an error, write `ERROR` in the response box.

# A

```
numbers = [1, 2, 5, 7, 2]
numbers.append(3)
print(numbers)
```

# B

```
counts = {"a": 1, "b": 2, "c": 10}
counts.pop("a")
print(counts)
```

# C

```
numbers = {4, 3, 1, 4, 3, 2}
numbers.add(10)
print(numbers)
```

# D

```
numbers = [3, 2, 10, 2]
print(numbers[4])
```

# E

```
numbers = (3, 2, 10, 2)
print(numbers[0])
```

Rubric:

- [ ]  +1 A's correct answer is `[1, 2, 5, 7, 2, 3]`
- [ ]  +1 B's correct answer is `{"b": 2, "c": 10}`
- [ ]  +1 C's correct answer is `{1, 2, 3, 4, 10}` (no repeated values, any order)
- [ ]  +1 D's correct answer is `ERROR`
- [ ]  +1 E's correct answer is `3`

